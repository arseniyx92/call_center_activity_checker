# Настройка Google Sheets для проверки врачей

## Описание

Модуль использует Google Sheets для хранения расписания врачей и проверки их доступности. Используются **две таблицы**: справочник врачей и матрица расписания.

## Структура таблиц

### 1. Лист "Врачи" - справочник врачей

Первая строка - заголовки, далее данные:

| ФИО врача | Специальность |
|-----------|---------------|
| Иванов И.И. | Терапевт |
| Петров П.П. | Кардиолог |
| Сидоров С.С. | Стоматолог |

**Колонки:**
- **ФИО врача** (колонка A): Полное имя врача
- **Специальность** (колонка B): Специальность врача (терапевт, кардиолог, стоматолог и т.д.)

**Примечание:** Названия колонок могут варьироваться:
- "ФИО врача" или "Имя" или "Врач"
- "Специальность" или "Специализация"

### 2. Лист "Расписание" - матрица расписания

Матрица с временем в строках и врачами в столбцах:

```
        | Время | Иванов И.И. | Петров П.П. | Сидоров С.С. |
        |-------|-------------|-------------|--------------|
        | 9:00  | 🟢          | 🔴          | 🔵           |
        | 10:00 | 🟢          | 🟢          | 🟢           |
        | 11:00 | 🟢          | 🔴          | 🟢           |
        | ...   | ...         | ...         | ...          |
        | 21:00 | 🟢          | 🟢          | 🔴           |
```

**Структура:**
- **Строка 1**: Заголовки (первая колонка пустая или "Время", далее ФИО врачей)
- **Столбец A**: Время (9:00, 10:00, 11:00, ..., 21:00) - по одному часу
- **Столбцы B, C, D, ...**: Врачи (ФИО должно совпадать с листом "Врачи")
- **Клетки на пересечении**: 
  - 🟢 **Зеленый цвет фона** = свободно
  - 🔴 **Красный цвет фона** = занято
  - 🔵 **Синий цвет фона** = выходной

**Важно:**
- Время с 9:00 до 21:00 (13 строк)
- Каждая строка - один час
- Цвет фона ячейки определяет статус доступности
- Имена врачей в заголовках должны совпадать с именами в листе "Врачи"

## Настройка Google Sheets API

### Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

### Шаг 2: Создание сервисного аккаунта

1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "Service Account"
3. Введите имя сервисного аккаунта (например, "call-center-bot")
4. Нажмите "Create and Continue"
5. Роль не требуется (можете пропустить)
6. Нажмите "Done"

### Шаг 3: Создание ключа для сервисного аккаунта

1. Найдите созданный сервисный аккаунт в списке
2. Нажмите на email сервисного аккаунта
3. Перейдите на вкладку "Keys"
4. Нажмите "Add Key" → "Create new key"
5. Выберите формат JSON
6. Нажмите "Create"
7. Файл JSON будет скачан автоматически

### Шаг 4: Настройка доступа к таблице

1. Откройте скачанный JSON файл
2. Скопируйте email сервисного аккаунта (поле `client_email`)
3. Откройте вашу Google таблицу с расписанием врачей
4. Нажмите "Share" (Поделиться)
5. Вставьте email сервисного аккаунта
6. Дайте права "Viewer" (Читатель) или "Editor" (Редактор), если нужно
7. Нажмите "Send"

### Шаг 5: Получение ID таблицы

1. Откройте вашу Google таблицу
2. Посмотрите в URL: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`
3. Скопируйте `SPREADSHEET_ID` (часть между `/d/` и `/edit`)

### Шаг 6: Настройка переменных окружения

1. Переместите скачанный JSON файл в корень проекта
2. Переименуйте его в `credentials.json` (или укажите путь в `.env`)
3. Добавьте в файл `.env`:

```env
# Google Sheets настройки
GOOGLE_SHEETS_SPREADSHEET_ID=ваш_spreadsheet_id_из_url
GOOGLE_SHEETS_CREDENTIALS_PATH=credentials.json
```

**Примечание:** Названия листов можно изменить в коде. По умолчанию:
- Лист "Врачи" - справочник врачей
- Лист "Расписание" - матрица расписания

**Пример:**
```env
GOOGLE_SHEETS_SPREADSHEET_ID=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
GOOGLE_SHEETS_CREDENTIALS_PATH=credentials.json
```

## Создание таблиц в Google Sheets

### Лист "Врачи"

1. Создайте новый лист или переименуйте существующий в "Врачи"
2. В первой строке добавьте заголовки:
   - A1: "ФИО врача"
   - B1: "Специальность"
3. Заполните данные о врачах

### Лист "Расписание"

1. Создайте новый лист или переименуйте существующий в "Расписание"
2. В первой строке (A1) можно оставить пустым или написать "Время"
3. Начиная с B1, укажите ФИО врачей (должны совпадать с листом "Врачи")
4. В колонке A, начиная со строки 2, укажите время:
   - A2: "9:00"
   - A3: "10:00"
   - A4: "11:00"
   - ...
   - A14: "21:00"
5. Для каждой ячейки на пересечении времени и врача:
   - Выделите ячейку
   - Установите цвет фона:
     - 🟢 Зеленый - если врач свободен в это время
     - 🔴 Красный - если врач занят в это время
     - 🔵 Синий - если врач в выходной в это время

**Пример форматирования:**
- Ячейка B2 (Иванов И.И., 9:00) - зеленый фон → свободен
- Ячейка B3 (Иванов И.И., 10:00) - красный фон → занят
- Ячейка C2 (Петров П.П., 9:00) - синий фон → выходной

## Проверка настройки

Запустите тестовый скрипт:

```python
from google_sheets import DoctorsSchedule

schedule = DoctorsSchedule()

# Проверка подключения
doctors = schedule.get_all_doctors()
print(f"Найдено врачей: {len(doctors)}")
for doctor in doctors:
    print(f"- {doctor['name']}, {doctor['specialty']}")

# Проверка поиска врача
result = schedule.check_doctor_availability(
    doctor_name="Иванов",
    specialty="Терапевт",
    time_slot="14"  # или "14:00"
)
print(result)
```

## Использование в коде

```python
from google_sheets import DoctorsSchedule

schedule = DoctorsSchedule()

# Проверка доступности врача
result = schedule.check_doctor_availability(
    doctor_name="Иванов И.И.",
    specialty="Терапевт",
    time_slot="14"  # или "14:00"
)

if result['available_at_time']:
    print(f"✅ {result['message']}")
else:
    print(f"❌ {result['message']}")
```

## Форматы данных

### Время

Формат: `HH` или `HH:MM` (24-часовой формат)
- Примеры: "9", "14", "14:00", "21"
- В расписании строки должны соответствовать времени с 9:00 до 21:00

### Цвета ячеек

Цвет фона определяет статус:
- **Зеленый** (`rgb(217, 234, 211)`) - свободно
- **Красный** (`rgb(244, 204, 204)`) - занято  
- **Синий** (`rgb(204, 224, 244)`) - выходной

**Примечание:** Если не удается определить цвет, используется fallback:
- Пустая ячейка или текст "свободно"/"free" → свободно
- Текст "занято"/"busy"/"занят" → занято
- Текст "выходной"/"holiday"/"вых" → выходной

## Устранение проблем

### Ошибка: "Файл credentials.json не найден"

- Убедитесь, что файл находится в корне проекта
- Или укажите правильный путь в `.env`: `GOOGLE_SHEETS_CREDENTIALS_PATH=путь/к/credentials.json`

### Ошибка: "Permission denied"

- Убедитесь, что сервисный аккаунт имеет доступ к таблице
- Проверьте, что email сервисного аккаунта добавлен в список редакторов/читателей таблицы

### Ошибка: "Spreadsheet not found"

- Проверьте правильность `GOOGLE_SHEETS_SPREADSHEET_ID` в `.env`
- Убедитесь, что сервисный аккаунт имеет доступ к таблице

### Ошибка: "Лист 'Врачи' не найден" или "Лист 'Расписание' не найден"

- Убедитесь, что листы называются именно "Врачи" и "Расписание"
- Или измените названия при инициализации:
  ```python
  schedule = DoctorsSchedule(
      doctors_sheet="Справочник",
      schedule_sheet="График"
  )
  ```

### Врач не найден

- Проверьте, что имя врача в транскрипции совпадает с именем в листе "Врачи"
- Проверьте, что имя врача есть в заголовках листа "Расписание"
- Проверьте регистр букв (поиск не чувствителен к регистру)
- Используется частичное совпадение (например, "Иванов" найдет "Иванов И.И.")

### Цвет ячейки не определяется

- Убедитесь, что установлен `google-api-python-client`: `pip install google-api-python-client`
- Проверьте, что цвета фона установлены правильно в Google Sheets
- Система автоматически использует fallback по тексту ячейки, если цвет не определен

### Время не найдено в расписании

- Убедитесь, что в листе "Расписание" есть строка с соответствующим временем (9:00-21:00)
- Проверьте формат времени в колонке A (должно быть "9:00", "10:00" и т.д.)
