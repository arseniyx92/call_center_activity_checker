# Call Center Activity Checker

Система мониторинга активности call-центра с интеграцией PBX Gravitel и Telegram-логированием.

## Описание

Проект предназначен для автоматического мониторинга звонков в call-центре через API Gravitel (PBX система). Система получает историю звонков, скачивает записи разговоров и отправляет уведомления администраторам через Telegram бота.

## Функциональность

- 📞 Получение истории звонков из PBX системы Gravitel
- 📥 Автоматическое скачивание записей звонков
- 📱 Логирование событий и уведомления в Telegram
- 🔔 Отправка сообщений администраторам через Telegram бота
- ⚙️ Гибкая настройка через переменные окружения

## Структура проекта

```
call_center_activity_checker/
├── main.py              # Главный файл приложения
├── hosted_pbx.py        # Модуль работы с API Gravitel (PBX)
├── tg_bot_core.py       # Основной модуль Telegram бота
├── tg_logger.py         # Модуль логирования в Telegram
├── stt.py               # Модуль распознавания речи (в разработке)
├── llm_corrector.py     # Модуль коррекции текста через LLM (в разработке)
├── requirements.txt     # Зависимости проекта
└── README.md            # Документация проекта
```

## Компоненты

### main.py
Главный файл приложения, который:
- Настраивает Telegram бота для логирования
- Получает историю звонков за вчерашний день
- Скачивает последнюю запись звонка
- Отправляет информацию о звонках в Telegram

### hosted_pbx.py
Модуль для работы с API Gravitel:
- `get_call_history()` - получение истории звонков за указанный период (today, yesterday, this_week, last_week, this_month, last_month)
- `download_recording()` - скачивание записи звонка по URL

**Поддерживаемые параметры запроса:**
- `period` - период выборки (today, yesterday, this_week, last_week, this_month, last_month)
- `start` - начало периода (опционально)
- `end` - конец периода (опционально)
- `type` - тип звонка (all, in, out, missed) (опционально)
- `limit` - ограничение количества записей (опционально)

### tg_bot_core.py
Основной модуль Telegram бота с командами:
- `/start` - запуск бота
- `/test_log` - тестирование системы логирования

### tg_logger.py
Модуль для отправки логов администраторам в Telegram:
- `set_application()` - установка экземпляра Telegram приложения
- `Log_in_tg()` - отправка сообщений всем администраторам

## Установка

### Требования

- Python 3.7+
- Аккаунт в Gravitel с API ключом
- Telegram бот токен
- Telegram ID администраторов

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd call_center_activity_checker
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```env
# Gravitel API настройки
GRAVITEL_DOMAIN=your_domain
GRAVITEL_API_KEY=your_api_key

# Telegram настройки
LOG_BOT_TOKEN=your_telegram_bot_token
ADMIN_TELEGRAM_USERS=123456789,987654321
```

### Получение необходимых данных

#### Gravitel API
1. Войдите в панель управления Gravitel
2. Перейдите в раздел API
3. Создайте API ключ
4. Укажите ваш домен в `GRAVITEL_DOMAIN`

#### Telegram бот
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота и укажите в `LOG_BOT_TOKEN`
3. Узнайте ваш Telegram ID (можно через [@userinfobot](https://t.me/userinfobot))
4. Укажите ID администраторов через запятую в `ADMIN_TELEGRAM_USERS`

## Использование

### Запуск основного приложения

```bash
python main.py
```

Приложение выполнит:
1. Получение истории звонков за вчерашний день
2. Скачивание последней записи звонка в папку `recordings/`
3. Отправку информации о последнем звонке в Telegram

### Запуск Telegram бота

```bash
python tg_bot_core.py
```

Бот будет доступен в Telegram и будет отвечать на команды:
- `/start` - приветствие и запуск бота
- `/test_log` - тестирование системы логирования

## Зависимости

- `requests>=2.25.1` - для HTTP запросов к API
- `python-dotenv>=0.19.0` - для работы с переменными окружения
- `python-telegram-bot==21.0` - для работы с Telegram Bot API
- `telegram` - дополнительная библиотека для Telegram

## Структура данных

### Формат ответа get_call_history()

```python
{
    'error': None,  # или строка с описанием ошибки
    'info': [
        {
            'id': 'call_id',
            'type': 'in' | 'out' | 'missed',
            'client': 'phone_number',
            'start': '2024-01-01T12:00:00',
            'wait': 5,  # секунды ожидания
            'duration': 120,  # секунды разговора
            'record': 'https://...'  # URL записи
        }
    ]
}
```

## Настройка

### Изменение периода выборки звонков

В файле `hosted_pbx.py` измените параметр `period`:

```python
payload = {
    "period": "today"  # или yesterday, this_week, last_week, this_month, last_month
}
```

### Настройка фильтров звонков

Раскомментируйте и настройте параметры в `hosted_pbx.py`:

```python
payload = {
    "period": "yesterday",
    "type": "in",  # только входящие звонки
    "limit": 50    # максимум 50 записей
}
```

## Разработка

### Модули в разработке

- `stt.py` - модуль для распознавания речи из записей звонков
- `llm_corrector.py` - модуль для коррекции текста через LLM

## Обработка ошибок

Система обрабатывает следующие типы ошибок:
- Ошибки подключения к API Gravitel
- Ошибки скачивания записей
- Ошибки отправки сообщений в Telegram
- Все ошибки логируются в Telegram администраторам

